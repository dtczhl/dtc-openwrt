#!/bin/sh

# kill all processes with the given program name
#
# Huanle Zhang at UC Davis
# www.huanlezhang.com


if [ $# -ne 1]; then
    echo "*** Error!"
    echo "dtcKillProgram program_name"
fi

ps | grep "$1" | grep -v grep | awk '{print $1}' | xargs -r kill -9
